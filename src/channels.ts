export type Channels = {
  JOIN: string
  CHAT: string
  LEAVE: string
  USER_ID: string
}

const CHANNELS = {
  JOIN: 'join',
  CHAT: 'chat',
  LEAVE: 'leave',
  USER_ID: 'userId',
}

export default CHANNELS
